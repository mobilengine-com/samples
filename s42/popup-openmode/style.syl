styles {
    style myPopup {
        calendar {
            eventBubble = myPopup
        }
        map {
            pointBubble = myPopup
        }
        timeline {
            eventBubble = myPopup
        }
        popup {
            content = myPopup
        }
        frame {
            frameWidth = 300px
            frameHeight = 200px
        }
    }
}