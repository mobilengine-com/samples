server program NotifyUsers for schedule * * * * * first run at 2100-01-01 00:00
// for form NotifyUsers
{
	let noti = {
		title: {default: "Tap to crash"},
		body: {default: "Tap to crash"},
		formToOpen: {
			android: "FormToOpenAndroid",
			ios: "FormToOpenIos"
		}
		// ,formArea: 42
		// ,formArea: "proba"
		,formArea: null
		// no formArea
	};
	Log(["Sending Notification", noti]);
	SendNotification(null, noti);
}