- use the Plcli plugin to test sending a dacs
- settings:
    - iep name: in
    - meta: purchase-order
    - content: use the contents of sample.dacs.xml

- if you're writing a plugin, use the PluginSupport.sendDacs() method