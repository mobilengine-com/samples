//# server program for_rfs for schedule * * * * * first run at 2100-01-01 00:00

{
    // prints 1,2,3
    Log("for (let i in [1,2,3])");
    Log("----------------------");
    for (var i in [1,2,3]) {
        Log(i);
    }

    // for..of does the same for lists

    // for..in and for..of throw a "map is not list" error for maps
}