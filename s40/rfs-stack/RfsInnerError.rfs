server program RfsInnerError
	for schedule * * * * * 
	first run at 2099-01-01 10:00
{
  var greeterBot = function(var firstname, var lastname)
  {
		Error("This is just a safety drill.");
  };
  var greeterBot2 = function(var firstname, var lastname)
  {
  		greeterBot("Petar", "Hoyt");
  };
  greeterBot2("Petar", "Hoyt");
}